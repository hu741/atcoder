#include <bits/stdc++.h> 
using namespace std;

int main(){

        int N;
        cin >> N;
        string s[N] = {};
        
        for(int i = 0;i<N;i++){
            cin >> s[i];

            sort(s.begin(),s.end());
        }

        for(i)
    




}